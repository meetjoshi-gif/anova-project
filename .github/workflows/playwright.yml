name: Run Playwright Tests

on:

  workflow_dispatch:
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  run_tests:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: Cache Node.js modules
        uses: actions/cache@v4
        with:
          path: ~/.npm
          key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-node-


      - name: Install Dependencies
        run: |
             npm ci
             npx playwright install

      - name: Install Netlify CLI
        run: npm install -g netlify-cli       

      - name: Run Playwright tests in headless mode
        run: |
          xvfb-run --auto-servernum --server-args="-screen 0 1920x1080x24" npx playwright test
        env:
          CI: true



      # ✅ Upload HTML Report
      - name: Upload Playwright Report
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: reports/

      # ✅ Upload Traces, Videos, Screenshots

      - name: Upload Test Results (Trace/Video/Screenshot)
        uses: actions/upload-artifact@v4
        with:
          name: test-results
          path: test-results/



      # ✅ Deploy HTML Report (Public)

      - name: Deploy Report to GitHub Pages
        if: always()
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./reports

      - name: Deploy Report to Netlify
        if: always()
        run: |
          netlify deploy \
          --dir=reports \
          --site=$NETLIFY_SITE_ID \
          --auth=$NETLIFY_AUTH_TOKEN \
          --prod
        env:
          NETLIFY_AUTH_TOKEN: nfp_kpAxsudkNMY3c8qkMbfBQSN2vScqkBLWbe86
          NETLIFY_SITE_ID: 8f3ea6ff-e3d0-41bd-b8e6-2bce67fa4470    
     

      # ✅ Email PASS

      - name: Send Email to Client (PASS)
        if: success()
        uses: dawidd6/action-send-mail@v4
        with:
          server_address: smtp.mailersend.net
          server_port: 2525
          secure: false
          username: MS_H24YF9@test-86org8emz50gew13.mlsender.net
          password: mssp.L66Tltr.z86org8vov0lew13.AhI2UJr
          subject: "Anova | Automation Test Execution Report - Passed"
          to: meet.joshi@pulsesolutions.net
          cc: ankita.mahadik@pulsesolutions.net
          from: MS_H24YF9@test-86org8emz50gew13.mlsender.net
          body: |

            <h2> Anova - Playwright Automation Test Execution Report</h2>

            <p>Hello Team,</p>

            <p>The automated test execution for the <b>Anova</b> project has been completed successfully.</p>

            <p><b>Execution Status:</b> PASSED</p>

            <h3>Reports</h3>

            <p>
            <a href="https://anova-html-report-ce92f9.netlify.app/">
            View Playwright HTML Report
            </a>
            </p>

            <h3>Execution Artifacts</h3>

            <p>
            <a href="https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}">
            Download Traces, Videos and Screenshots
            </a>
            </p>

            <p>Please review the report for detailed execution results.</p>

            <br>

            <p>
            Best Regards,<br>
            Meet Joshi<br>
            QA Automation Engineer
            </p>

      # ❌ Email FAIL (Detailed)



      - name: Send Email to Developer (FAIL)
        if: failure()
        uses: dawidd6/action-send-mail@v4
        with:
          server_address: smtp.mailersend.net
          server_port: 2525
          secure: false
          username: MS_H24YF9@test-86org8emz50gew13.mlsender.net
          password: mssp.L66Tltr.z86org8vov0lew13.AhI2UJr
          subject: "Anova | Automation Test Execution Report - Failed"
          to: meet.joshi@pulsesolutions.net
          from: MS_H24YF9@test-86org8emz50gew13.mlsender.net
          body: |

            Anova - Playwright Automation Test Execution Report - Failed

            📊 Summary:
                <h2> Anova - Playwright Automation Test Execution Report</h2>

            <p>Hello Team,</p>

            <p>The automated test execution for the <b>Anova</b> project has completed with failures.</p>

            <p><b>Execution Status:</b> FAILED</p>

            <p>
            One or more test cases failed during execution.
            Please review the report and execution artifacts for failure analysis.
            </p>

            <h3>Reports</h3>

            <p>
            <a href="https://anova-html-report-ce92f9.netlify.app/">
            View Playwright HTML Report
            </a>
            </p>

            <h3>Execution Artifacts</h3>

            <p>
            <a href="https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}">
            Download Traces, Videos and Screenshots
            </a>
            </p>

            <br>

            <p>
            Best Regards,<br>
            Meet Joshi<br>
            QA Automation Engineer
            </p>


           